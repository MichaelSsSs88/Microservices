spring:
  profiles:
    active: bike-service
  application:
    name: bike-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081/
      fail-fast: true
  #server:
  #port: 8003

